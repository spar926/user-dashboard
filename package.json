{
  "name": "user-dashboard",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:emu": "firebase emulators:start --only firestore"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/socket.io": "^3.0.1",
    "concurrently": "^9.2.0",
    "firebase-tools": "^14.12.1"
  },
  "dependencies": {
    "@nestjs/platform-socket.io": "^11.1.6",
    "@nestjs/websockets": "^11.1.6",
    "socket.io": "^4.8.1"
  }
}
